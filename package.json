{
  "name": "nusabit-studio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx netlify dev",
    "start": "npx netlify dev",
    "build": "npx netlify build --offline",
    "deploy:preview": "npx netlify deploy",
    "deploy:prod": "npx netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
